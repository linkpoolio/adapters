{
  "$id": "https://external-adapters.chainlinklabs.com/schemas/nft-metrics-adapter.json",
  "title": "NFT Metrics Adapter",
  "description": "The external adapter for the LinkPool NFT Metrics API.",
  "required": ["ACCESS_KEY", "SECRET_KEY"],
  "type": "object",
  "properties": {
    "ACCESS_KEY": {
      "type": "string",
      "description": "The Access Key that can be obtained from LinkPool."
    },
    "SECRET_KEY": {
      "type": "string",
      "description": "The Secret Key that can be obtained from LinkPool."
    }
  },
  "allOf": [
    {
      "$ref": "https://external-adapters.chainlinklabs.com/schemas/ea-bootstrap.json"
    }
  ]
}
