{
  "$id": "https://external-adapters.chainlinklabs.com/schemas/anchain-adapter.json",
  "title": "Anchain Adapter",
  "description": "The AnChain adapter checks if an address appears in the OFAC Anti-Terrorism/Anti-Money Laundering list and return the category in which the address belongs.",
  "required": ["API_KEY"],
  "type": "object",
  "properties": {
    "API_KEY": {
      "type": "string",
      "description": "API key to use"
    }
  },
  "allOf": [
    {
      "$ref": "https://external-adapters.chainlinklabs.com/schemas/ea-bootstrap.json"
    }
  ]
}
