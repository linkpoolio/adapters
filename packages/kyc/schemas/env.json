{
  "$id": "https://external-adapters.chainlinklabs.com/schemas/kyc-adapter.json",
  "title": "kyc Adapter",
  "description": "This adapter demonstrates leveraging two data providers for the same data types.",
  "required": [],
  "type": "object",
  "properties": {
    "CIPHERTRACE_ACCESS_KEY": {
      "description": "The Ciphertrace's API Access Key",
      "required": false,
      "type": "string"
    },
    "CIPHERTRACE_SECRET_KEY": {
      "description": "The Ciphertrace's API Secret Key",
      "required": false,
      "type": "string"
    },
    "EVEREST_API_KEY": {
      "description": "The Everest's API Secret Key",
      "required": false,
      "type": "string"
    },
    "API_PROVIDER": {
      "description": "The data provider",
      "required": true,
      "type": "string"
    }
  },
  "allOf": [
    {
      "$ref": "https://external-adapters.chainlinklabs.com/schemas/ea-bootstrap.json"
    }
  ]
}
