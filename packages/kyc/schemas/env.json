{
  "$id": "https://external-adapters.chainlinklabs.com/schemas/kyc-adapter.json",
  "title": "kyc Adapter",
  "description": "This adapter demonstrates leveraging two data providers for the same data types.",
  "required": [],
  "type": "object",
  "properties": {
    "ACCESS_KEY": {
      "description": "The data provider's Access Key",
      "required": true,
      "type": "string"
    },
    "SECET_KEY": {
      "description": "The data provider's Secret Key",
      "required": true,
      "type": "string"
    },
    "API_PROVIDER": {
      "description": "The data provider",
      "required": true,
      "type": "string"
    }
  },
  "allOf": [
    {
      "$ref": "https://external-adapters.chainlinklabs.com/schemas/ea-bootstrap.json"
    }
  ]
}
