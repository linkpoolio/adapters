{
  "$id": "https://external-adapters.chainlinklabs.com/schemas/enetscores-adapter.json",
  "title": "Chainlink External Adapter for Enetscores",
  "required": ["API_TOKEN", "API_USERNAME"],
  "type": "object",
  "properties": {
    "API_TOKEN": {
      "required": true,
      "type": "string",
      "description": "The `token` query string parameter required for API authentication. Contact the data provider"
    },
    "API_USERNAME": {
      "required": true,
      "type": "string",
      "description": "The `username` query string parameter required for API authentication. Contact the data provider"
    }
  },
  "allOf": [
    {
      "$ref": "https://external-adapters.chainlinklabs.com/schemas/ea-bootstrap.json"
    }
  ]
}
